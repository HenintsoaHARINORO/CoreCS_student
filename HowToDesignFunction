;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname HowToDesignFunction) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Number-> Number (Signature)
;; produce 2 times the given number (Purpose)

(check-expect (double 3) 6)  ;Example


;(define (double n) 0) ;this is the stub (stub)

;(define (double n)  ;template
 ; (... n))

(define (double n) 
  (* 2 n))